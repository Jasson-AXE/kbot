# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
#image:
#  repository: "jassonaxe"
  # Overrides the image tag whose default is the chart appVersion.
#  tag: "v1.0.5-f8b34c7"
#  arch: amd64
Image:
  registry: "ghcr.io"
  repository: "jassonaxe/kbot"
  tag: "v1.0.5-97cd54c"
  os: linux
  arch: amd64
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
