name: KBOT-CICD

on: push

jobs:       #Create new job with name CI
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run test
        run : make test
